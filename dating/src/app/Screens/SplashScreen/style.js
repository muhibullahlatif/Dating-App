import React from 'react';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    MainContainer: {
        width: '100%',
        height: '100%',
    },
    loader: {
        position: 'absolute',
        bottom: 20,
        alignSelf: 'center',
      },
});